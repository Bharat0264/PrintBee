"use client";
import { useState } from "react";
declare global { interface Window { Razorpay?: any } }
export default function PurchaseButton({projectId}:{projectId:string}){const [message,setMessage]=useState("");const buy=async()=>{const r=await fetch("/api/projects/purchase",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({projectId})});const d=await r.json();if(!r.ok)return setMessage(d.error??"Could not start payment");if(!window.Razorpay){const s=document.createElement("script");s.src="https://checkout.razorpay.com/v1/checkout.js";await new Promise((ok,no)=>{s.onload=ok;s.onerror=no;document.head.appendChild(s)});}new window.Razorpay({key:d.keyId,amount:d.amount,currency:"INR",name:"PrintBee Projects",description:`${d.title} · includes ₹${(d.gatewayFee/100).toFixed(2)} payment handling`,order_id:d.razorpayOrderId,handler:async(x:any)=>{const v=await fetch("/api/projects/purchase/verify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({orderId:d.orderId,...x})});const out=await v.json();setMessage(v.ok?"Payment verified. Your secure downloads are unlocked.":out.error??"Verification failed");}}).open()};return <><p className="project-fee-note">A 2.36% payment handling charge is added only for project purchases.</p><button className="project-buy" onClick={buy}>Buy securely</button>{message&&<p>{message}</p>}</>}
