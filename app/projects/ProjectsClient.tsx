"use client";
import { FormEvent, useEffect, useState } from "react";
const cats=["AI","Machine Learning","Data Science","Web Development","Mobile Development","IoT","Cybersecurity","Cloud","Blockchain","Big Data","Electronics","Robotics","Other"];
export default function ProjectsClient({view}:{view:"home"|"buy"|"sell"|"build"|"mine"|"requests"}) { const [items,setItems]=useState<any[]>([]),[message,setMessage]=useState(""),[q,setQ]=useState(""); const load=async()=>{const url=view==="buy"?`/api/projects?q=${encodeURIComponent(q)}`:view==="mine"?"/api/projects/my":view==="requests"?"/api/projects/build-requests":""; if(url){const r=await fetch(url);const d=await r.json();setItems(d.projects??d.requests??[]);}}; useEffect(()=>{void load()},[view,q]); const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget); const body=Object.fromEntries(f); const endpoint=view==="sell"?"/api/projects":"/api/projects/build-requests"; if(view==="sell")Object.assign(body,{ownershipConfirmed:f.get("ownershipConfirmed")==="on",features:String(f.get("features")??"").split(","),techStack:String(f.get("techStack")??"").split(",")}); const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();setMessage(r.ok?"Submitted for PrintBee review.":d.error??"Please try again.");if(r.ok)e.currentTarget.reset();}; if(view==="home")return <main className="projects"><section className="projects-hero"><p>PRINTBEE PROJECTS</p><h1>Turn ideas into projects.</h1><span>Buy reference implementations, share something you built, or get technical development support.</span></section><section className="projects-grid">{[["Buy projects","Browse verified learning-ready implementations.","/projects/buy"],["Sell a project","Submit your work for PrintBee review and listing.","/projects/sell"],["Build my project","Get a transparent quote for development support.","/projects/build"]].map(([a,b,c])=><a href={c} key={a}><b>{a}</b><small>{b}</small><em>Explore →</em></a>)}</section><p className="projects-note">Project materials are for learning, reference, development and authorized use. Follow your institution’s academic-integrity policies.</p></main>;
if(view==="buy")return <main className="projects"><h1>Verified project implementations</h1><input placeholder="Search projects or technology" value={q} onChange={e=>setQ(e.target.value)}/><section className="market-grid">{items.length?items.map(p=><article key={p.id}><small>{p.category} · Verified</small><h2>{p.title}</h2><p>{p.short_description}</p><div>{JSON.parse(p.tech_stack_json??"[]").map((x:string)=><i key={x}>{x}</i>)}</div><strong>₹{(p.final_price_paise/100).toLocaleString("en-IN")}</strong><a href={`/projects/project/${p.slug}`}>View project →</a></article>):<p>No projects available yet.</p>}</section></main>;
if(view==="mine"||view==="requests")return <main className="projects"><h1>{view==="mine"?"My projects":"My build requests"}</h1><section className="market-grid">{items.length?items.map(x=><article key={x.id}><small>{x.status.replaceAll("_"," ")}</small><h2>{x.title}</h2><p>{x.category}</p>{x.proposed_price_paise!=null&&<strong>Proposed ₹{x.proposed_price_paise/100}</strong>}{x.quoted_price_paise!=null&&<strong>Quote ₹{x.quoted_price_paise/100}</strong>}</article>):<p>Nothing here yet.</p>}</section></main>;
const sell=view==="sell"; return <main className="projects"><h1>{sell?"Sell a project":"Build my project"}</h1><form className="project-form" onSubmit={submit}><input name="title" required placeholder="Project title"/><select name="category" required><option value="">Choose category</option>{cats.map(x=><option key={x}>{x}</option>)}</select>{sell?<><textarea name="shortDescription" required minLength={20} placeholder="Short description"/><textarea name="problemStatement" placeholder="Problem statement"/><textarea name="description" placeholder="Detailed description"/><input name="techStack" placeholder="Technology stack, comma separated"/><input name="features" placeholder="Features, comma separated"/><input name="requestedPrice" type="number" min="0" required placeholder="Expected selling price (₹)"/><label><input name="ownershipConfirmed" type="checkbox" required/> I own this work or have rights to distribute it.</label></>:<><input name="name" required placeholder="Your name"/><textarea name="problemStatement" required minLength={15} placeholder="Problem statement"/><textarea name="requirements" required minLength={20} placeholder="Detailed requirements"/><input name="preferredTechnology" placeholder="Preferred technology"/><input name="requestedDeadline" placeholder="Expected deadline"/></>}<button>Submit for review</button></form>{message&&<p>{message}</p>}</main>; }
